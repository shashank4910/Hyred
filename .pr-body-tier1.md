## Summary
- Ship **Premium Tier 1**: Match Intelligence (`/api/match/[id]/verdict`), Interview Prep Pack (`/api/match/[id]/prep`), Resume Studio Pro quotas + version history on resume API
- Add migration **0015** (`premium_subscriptions`, usage ledger, `resume_versions`, `match_verdicts`, `interview_prep_packs`) and `lib/premium.ts` entitlement helpers
- Job detail UI: verdict card, prep card, resume version history, premium quota toasts
- Docs: `CONTEXT.md`, `AGENTS.md` index, Session 27 in `session-log.md`, `docs/features-jun26-to-be-built.md`

## Test plan
- [x] `npm run typecheck`
- [x] `npx vitest run tests/unit/premium.test.ts tests/unit/match-intelligence.test.ts tests/unit/interview-prep.test.ts` (13/13)
- [ ] Run migration `0015_hyred_premium_tier1.sql` in Supabase SQL Editor (required before features work in prod)
- [ ] Job detail: free user sees locked Match Intelligence; 1 lifetime prep pack
- [ ] Insert `premium_subscriptions` row for test profile to unlock verdict and higher quotas

## Post-merge manual step
Run `supabase/migrations/0015_hyred_premium_tier1.sql` in Supabase — tables must exist before APIs work.
